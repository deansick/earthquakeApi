== README

This is a pretty bare-bones implementation of a service to store and query recent earthquakes. It depends on the <tt>earthdistance</tt> module for <tt>postgresql</tt>

To come:
Some sort of map-based visualization of earthquake data.
